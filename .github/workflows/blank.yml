name: update discovery
on:
  schedule:
    - cron:  '0,5,10,15,20,25,30,35,40,45,50,55 * * * *'
  push:
      branches:
          - master
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: fetch discovery
      run: fetch-discovery.sh
